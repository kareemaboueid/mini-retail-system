
/**
 * ## User DB Structure
 * ```
 * coll: `users`
 * model: `User`
 * ```
 */
const user = {
  /** coll: `users` */
  coll: String('users'),
  /** model: `User` */
  model: String('User'),

}

export { user };